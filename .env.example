# Environment Variables Example
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# =================================================================
# CRITICAL SECURITY - JWT Secret (REQUIRED)
# =================================================================
# Generate a secure random secret with at least 32 characters
# Command: openssl rand -base64 32
# This is used to sign JWT authentication tokens
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long

# =================================================================
# Database Configuration (REQUIRED)
# =================================================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
# For local development, you might use:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/portfolio
# For production (e.g., Vercel Postgres):
# DATABASE_URL=postgres://user:pass@host.postgres.vercel-storage.com:5432/verceldb?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# =================================================================
# Admin Credentials (REQUIRED)
# =================================================================
# Admin username (minimum 3 characters)
ADMIN_USERNAME=admin

# Admin password (MUST be a bcrypt hash, NOT plain text!)
# Generate with: npm run hash-password
# Enter your desired password when prompted, then copy the hash here
ADMIN_PASSWORD=$2b$12$examplehashwillbelongerthan60characters

# =================================================================
# File Storage (REQUIRED)
# =================================================================
# Vercel Blob Storage token for file uploads
# Get this from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_yourtoken

# =================================================================
# Optional Configuration
# =================================================================

# Node environment (automatically set by Vercel in production)
# NODE_ENV=production

# =================================================================
# Setup Instructions
# =================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Generate JWT_SECRET: openssl rand -base64 32
# 3. Set your DATABASE_URL
# 4. Choose your ADMIN_USERNAME
# 5. Generate ADMIN_PASSWORD hash: npm run hash-password
# 6. Get your BLOB_READ_WRITE_TOKEN from Vercel
# 7. Never commit .env.local to git!

# =================================================================
# Security Notes
# =================================================================
# - JWT_SECRET must be at least 32 characters
# - ADMIN_PASSWORD must be a bcrypt hash (starts with $2b$ or $2a$)
# - All values should be kept secret and never committed to git
# - In production, set these in Vercel Dashboard → Settings → Environment Variables
# - Rotate JWT_SECRET and passwords regularly
# - Use different values for development and production
