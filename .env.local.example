# Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# ============================================
# DATABASE
# ============================================
# Your Neon Postgres connection string
# Get this from: https://console.neon.tech/
DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require

# ============================================
# AUTHENTICATION
# ============================================
# JWT secret for token signing (generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this

# Admin username (plain text)
ADMIN_USERNAME=admin

# Admin password (MUST be bcrypt hashed!)
# Generate hash with: node scripts/hash-password.js yourpassword
# DO NOT use plain text password here!
ADMIN_PASSWORD=$2b$12$your-hashed-password-here

# ============================================
# FILE STORAGE
# ============================================
# Vercel Blob storage token
# Get this from Vercel Dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxx

# ============================================
# IMPORTANT NOTES
# ============================================
# 1. Never commit .env.local to Git (it's in .gitignore)
# 2. Add these same variables to Vercel dashboard for production
# 3. ADMIN_PASSWORD must be a bcrypt hash, not plain text
# 4. Use the hash-password.js script to generate the password hash
